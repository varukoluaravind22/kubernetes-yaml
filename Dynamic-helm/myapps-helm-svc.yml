{{- range $key, $app := .Values.applications }}
apiVersion: v1 
kind: Service
metadata: 
  name: {{.$app.name}}
kind:
  type: {{.$app.type}}
  selector:
    {{toYaml .$app.labels | nindent 4}}
  ports:
  - port: {{.$app.port}}
    nodePort: {{$app.nodePort}}
{{ - end }}