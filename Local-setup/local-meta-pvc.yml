apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: meta_pvc
spec:
  storageClassName: manual
  volumename: nfs-pv
  accessModes:
  - ReadWriteMany
  resourse:
    requests:
      storage: 4Gi