apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: meta_pvc
spec:
  storageClassName: manual
  volumename: nfs-meta-pv
  accessModes:
  - ReadWriteOnce
  resourse:
    requests:
      storage: 4Gi